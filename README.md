# go-ai

브라우저에서 실행되는 **2D 파동 시뮬레이션** 예제입니다.

## 기능
- 클릭으로 파동 펄스 생성
- 진동 소스(주파수 조절) on/off
- 시간 배속(스텝/프레임) 조절
- 파동 속도, 감쇠, 펄스 세기 조절
- 장애물 그리기/지우기/전체 삭제

## 실행 방법
정적 파일이므로 아무 HTTP 서버로 실행하면 됩니다.

예시:
```bash
python3 -m http.server 8000
```

그 다음 브라우저에서 `http://localhost:8000` 접속.
